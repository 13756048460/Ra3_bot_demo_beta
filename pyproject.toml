[project]
name = "ra3_bot_demo_beta"
version = "0.1.0"
description = "ra3_bot_demo_beta"
readme = "README.md"
requires-python = ">=3.8, <4.0"

[tool.nonebot]
adapters = [
    { name = "OneBot V11", module_name = "nonebot.adapters.onebot.v11" }
]
plugins = ["nonebot_plugin_localstore"]
plugin_dirs = ["src/plugins"]
builtin_plugins = []
